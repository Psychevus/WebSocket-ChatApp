apiVersion: v1
kind: Service
metadata:
  name: {{ include "chatapp.fullname" . }}-redis
spec:
  ports:
    - port: 6379
      targetPort: 6379
  selector:
    app: {{ include "chatapp.name" . }}-redis
