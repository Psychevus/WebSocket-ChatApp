apiVersion: v1
kind: Service
metadata:
  name: {{ include "chatapp.fullname" . }}-mysql
spec:
  ports:
    - port: 3306
      targetPort: 3306
  selector:
    app: {{ include "chatapp.name" . }}-mysql
